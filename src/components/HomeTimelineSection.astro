---
import type { SiteLang } from "@/lib/blog";
import { getNewsExcerpt, renderNewsBody, type NewsEntry } from "@/lib/news";
import Button from "@/components/Button.astro";

interface Props {
  lang: SiteLang;
  news: NewsEntry[];
}

const { lang, news } = Astro.props;

const copy = {
  zh: {
    eyebrow: "Timeline / News",
    title: "近期动态",
    description: "",
    empty: "当前还没有新闻，稍后再来看看。",
    cta: "查看全部动态",
  },
  en: {
    eyebrow: "Timeline / News",
    title: "Latest Updates",
    description: "",
    empty: "No news posts yet. Check back soon.",
    cta: "View all updates",
  },
  ja: {
    eyebrow: "Timeline / News",
    title: "最近の動向",
    description: "",
    empty: "現在ニュースはありません。後ほどご確認ください。",
    cta: "すべての動向を見る",
  },
  ko: {
    eyebrow: "Timeline / News",
    title: "최근 소식",
    description: "",
    empty: "아직 소식이 없습니다. 나중에 다시 확인해 주세요.",
    cta: "모든 소식 보기",
  },
} as const;

const text = copy[lang];
---

<section id="news" class="timeline-section">
  <div class="timeline-heading">
    <span>{text.eyebrow}</span>
    <div class="timeline-heading-row">
      <h2>{text.title}</h2>
      <Button href={`/${lang}/news`}>{text.cta}</Button>
    </div>
  </div>

  {
    news.length ? (
      <ol class="timeline-list">
        {news.map((item, index) => (
          <li
            class:list={[
              "timeline-item",
              index % 2 === 1 && "timeline-item-reverse",
            ]}
          >
            <div class="timeline-entry">
              <time class="timeline-date" datetime={item.date.toISOString()}>
                {item.rawDate}
              </time>
              <p
                class="timeline-body"
                set:html={renderNewsBody(getNewsExcerpt(item.body))}
              />
            </div>
            <div class="timeline-axis" aria-hidden="true">
              <span class="timeline-node" />
            </div>
          </li>
        ))}
      </ol>
    ) : (
      <p class="timeline-empty">{text.empty}</p>
    )
  }
</section>

<style>
  .timeline-section {
    width: 100%;
    margin: 0;
    padding: clamp(1.6rem, 4vw, 2.8rem);
    border-radius: 2.25rem;
    border: 1px solid var(--border-color);
    overflow: visible;
    background:
      radial-gradient(
        circle at top left,
        color-mix(in srgb, var(--accent-color) 14%, transparent),
        transparent 26%
      ),
      linear-gradient(
        160deg,
        color-mix(in srgb, var(--accent-color) 7%, transparent),
        transparent 28%
      ),
      color-mix(in srgb, var(--bg-color) 90%, var(--bg-color-dimmer));
    box-sizing: border-box;
    box-shadow: 0 28px 60px
      color-mix(in srgb, var(--anti-bg-color) 7%, transparent);
  }

  .timeline-heading {
    display: grid;
    gap: 0.85rem;
    margin-bottom: 2rem;
  }

  .timeline-heading span {
    color: var(--sub-text-color);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    font-size: 0.82rem;
  }

  .timeline-heading-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem 1.5rem;
    flex-wrap: wrap;
  }

  .timeline-heading h2 {
    margin: 0;
    flex: 1 1 auto;
    min-width: 0;
    font-size: clamp(1.8rem, 4vw, 3rem);
  }

  .timeline-heading p,
  .timeline-empty {
    margin: 0;
    color: var(--sub-text-color);
    line-height: 1.75;
  }

  .timeline-heading-row :global(.button-container) {
    flex-shrink: 0;
    width: fit-content;
    margin-left: auto;
  }

  .timeline-list {
    list-style: none;
    padding: 0;
    margin: 1rem 0 0;
    display: grid;
    grid-template-columns: repeat(10, minmax(0, 1fr));
    gap: 0.9rem;
    position: relative;
    align-items: center;
    overflow: visible;
  }

  .timeline-list::before {
    content: "";
    position: absolute;
    z-index: 0;
    left: 0;
    right: 0;
    top: 50%;
    height: 2px;
    transform: translateY(-50%);
    background: linear-gradient(
      90deg,
      color-mix(in srgb, var(--accent-color) 35%, transparent),
      color-mix(in srgb, var(--accent-color) 68%, var(--border-color)),
      color-mix(in srgb, var(--accent-color) 35%, transparent)
    );
  }

  .timeline-item {
    grid-column: span 2;
    min-width: 0;
    display: grid;
    grid-template-rows: minmax(5.5rem, 1fr) 28px minmax(5.5rem, 1fr);
    position: relative;
    overflow: visible;
  }

  .timeline-entry {
    display: grid;
    gap: 0.45rem;
    min-height: 0;
    padding: 0.2rem 0.35rem 0.2rem 0;
    box-sizing: border-box;
    position: relative;
    z-index: 1;
    width: calc(150% + 0.9rem);
    max-width: none;
    pointer-events: auto;
  }

  .timeline-item-reverse .timeline-entry {
    grid-row: 3;
    align-self: start;
    align-content: start;
  }

  .timeline-item:not(.timeline-item-reverse) .timeline-entry {
    grid-row: 1;
    align-self: end;
    align-content: end;
  }

  .timeline-item:last-child .timeline-entry {
    width: 100%;
  }

  .timeline-date {
    color: var(--accent-color);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    display: block;
  }

  .timeline-body {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.4;
    color: var(--text-color);
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 3;
    overflow: hidden;
  }

  .timeline-body :global(a) {
    color: var(--accent-color);
    text-decoration: none;
    font-weight: 600;
  }

  .timeline-body :global(a:hover) {
    text-decoration: underline;
  }

  .timeline-axis {
    grid-row: 2;
    position: relative;
    z-index: 2;
    display: flex;
    justify-content: center;
    align-items: center;
    pointer-events: none;
  }

  .timeline-node {
    z-index: 1;
    width: 16px;
    height: 16px;
    border-radius: 999px;
    background: var(--accent-color);
    border: 4px solid var(--bg-color);
    box-shadow:
      0 0 0 6px color-mix(in srgb, var(--accent-color) 12%, transparent),
      0 8px 18px color-mix(in srgb, var(--accent-color) 24%, transparent);
  }

  .timeline-item:nth-child(n + 6) {
    display: none;
  }

  @media (max-width: 1200px) {
    .timeline-list {
      min-width: 0;
      display: grid;
      grid-auto-flow: row;
      grid-auto-columns: auto;
      grid-template-columns: 1fr;
      gap: 0.48rem;
      padding: 0;
      margin-top: 1.25rem;
    }

    .timeline-list::before {
      left: 12px;
      right: auto;
      top: 0;
      bottom: 0;
      width: 2px;
      height: auto;
      transform: none;
      background: linear-gradient(
        180deg,
        color-mix(in srgb, var(--accent-color) 35%, transparent),
        color-mix(in srgb, var(--accent-color) 68%, var(--border-color)),
        color-mix(in srgb, var(--accent-color) 35%, transparent)
      );
    }

    .timeline-item,
    .timeline-item-reverse {
      grid-column: auto;
      min-width: 0;
      grid-template-columns: 24px minmax(0, 1fr);
      grid-template-rows: auto;
      gap: 0.57rem;
      align-items: start;
      min-height: 0;
    }

    .timeline-item:nth-child(n + 5),
    .timeline-item:nth-child(n + 6) {
      display: grid;
    }

    .timeline-item:not(.timeline-item-reverse) .timeline-entry,
    .timeline-entry,
    .timeline-item-reverse .timeline-entry {
      grid-column: 2;
      grid-row: auto;
      width: 100%;
      max-width: none;
      min-height: auto;
      align-self: auto;
      align-content: start;
      padding: 0 0 0.6rem;
    }

    .timeline-item:last-child .timeline-entry {
      width: 100%;
      padding-bottom: 0;
    }

    .timeline-axis,
    .timeline-item-reverse .timeline-axis {
      grid-column: 1;
      grid-row: auto;
      min-height: 0;
      align-items: flex-start;
      justify-content: center;
      padding-top: 0.15rem;
    }

    .timeline-node {
      width: 14px;
      height: 14px;
      border-width: 3px;
      box-shadow:
        0 0 0 4px color-mix(in srgb, var(--accent-color) 12%, transparent),
        0 6px 14px color-mix(in srgb, var(--accent-color) 20%, transparent);
    }

    .timeline-body {
      -webkit-line-clamp: unset;
    }
  }
</style>
